# Debate 008 R001 - CODEX

Technically best disposition: D immediately, with B as the bookkeeping step. Keep the 787 inserted LOC (`room_dispatch.c` +682, `.h` +105 from `HEAD~5..HEAD`), relabel the work under real Phase 4 tasks, and consume the relevant leaves in Task 4.2 bombable walls. A is waste: it removes verified substrate from two files while preserving `Title.md` only because it changes nothing live. C is premature: `src/gen/z_07.c:305-444` has the `NATIVE_ROOM` gate and some wrappers, but many z_05 leaf paths still marshal through `src/c_shims.asm` into `src/gen/z_05.c`, which forwards to oracle `roomrt_*` (`z05_set_door_flag` at 41-43, `z05_check_secret_trigger_ringleader` at 289-290). Promotion is not one macro; it needs wrapper/manifest/c_shim reroutes plus parity baselines.

MATCH sample is good enough to keep. `room_set_door_flag` in `room_dispatch.c:1066-1072` matches oracle `room_runtime.c:84-89` and NES `Z_05.asm:2112-2115`: get room flags, OR `LevelMasks,X`, store through `$00/$01,Y`. `room_check_secret_trigger_ringleader` at `room_dispatch.c:1197-1211` matches oracle `room_runtime.c:215-230` and NES `Z_05.asm:2427-2461`: first-slot nonmonster false, reverse scan slots, ignore empty/nonmonster/dying, set metastate `$10`, return carry set. `room_update_mode2_load` at `room_dispatch.c:477-513` matches oracle `room_mode_runtime.c:317-320` plus `room_load_runtime.c:299-325`, and NES `Z_07.asm:1667-1671`/`Z_06.asm:202-234`, with deliberate Genesis substitutions for bank-window load and direct Q2 blob access. That one is semantic MATCH, not promotion-ready proof.

Costs: A touches 787 LOC deleted, `Title.md` sha risk near zero, RoomRom loses linked native-room test surface (`RoomRom/build.bat:147-149`). B touches 0 code LOC plus a small audit/plan note, no `Title.md` risk, no RoomRom regression. C touches at least build flags plus roughly dozens to hundreds of wrapper/c_shim/manifest lines; `Title.md` sha 56f1e2f7681562fc is intentionally at risk and the RoomRom/Title regression surface becomes full room-mode/door/secret dispatch. D touches the real Task 4.2 call sites and tests, likely ~80-150 feature LOC, while keeping `Title.md` gated off and RoomRom coverage focused.

Gemini's "quarantine until SRAM/world-state" is too passive; these leaves already write the NES flag cells they model. Sonnet is right that C is unsafe, but too coarse: `room_update_mode2_load` is wired through `z07_`; the darker problem is the z05 oracle-forwarder layer.
