# Phase 11 Task 11.2 — Intro Story

- **NES source**: `reference/aldonunez/Z_05.asm` story-scroll
                  routine + item-showcase + loop-back-to-title.
                  Memory `project_intro_loop` documents phase
                  machine: title → fade → black → story → items →
                  loop. Memory `project_intro_item_flash` pins
                  heart/container 8-frame flash cadence (FrameCounter
                  bit 3).
- **Drained C**:  `src/frontend/intro/intro_story.c` (12.7 KB) —
                  story render + scroll machine; `intro_phase.c`
                  governs phase transitions; `intro_handoff.c` does
                  the FS → title return; `intro_main.c` is the
                  entry-point + frame dispatch.
                  Assets: `data/intro/intro_story_tilemap.c`,
                  `intro_treasures_tilemap.c`,
                  `intro_combined_palette.c` etc. — all linked into
                  Debug.md via `TITLE_C_SOURCES`.
- **Coverage**:   FULL (substrate) — story scroll TU, item showcase
                  data TUs, fade TU, loop-back hook all shipped.
                  Memory `project_title_story_crash` flagged a
                  historical crash during story scroll; needs verify
                  against current build.
- **Stance**:     ADOPT — story surface is finalized Redux art +
                  scroll cadence; verify locks current behavior.

## Verify checklist (master plan)

| Item                         | Probe / Evidence                                |
|------------------------------|--------------------------------------------------|
| Title fade                   | bundled in title→story phase machine transition |
| Story scroll                 | `tools/probes/baselines/intro_story_p1.bin`     |
| Item showcase                | bundled (heart/container flash per `project_intro_item_flash`) |
| Loop back to title           | `intro_phase.c` phase machine final state       |
| Start exits from every phase | `intro_main.c` input handling                    |
| Assets generated by builder  | `data/intro/intro_*` C TUs in `TITLE_C_SOURCES`  |

## Active probes

`intro_story_p1` baselined; live capture is the frame-700 snapshot
in `capture_canonical.lua` pass-A (cold boot, after fadeout + black,
into story).

`intro_story_loop` per master plan NOT baselined — additional probe
needed for the full attract-loop cycle. Tracked as
`phase11_extra_probes_baseline` deferral below.

## Status

CLOSE (with live-capture + extra-probes deferrals) — Task 11.2 Intro
Story substrate landed + intro_story_p1 baseline locked.
intro_story_loop probe + capture pending.
